{
  "scripts": {
    "install": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "validate": "cd backend && npm run valid",
    "backend": "cd backend && npm run start",
    "frontend": "cd frontend && npm run dev",
    "all": "concurrently \"npm run validate\" \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
